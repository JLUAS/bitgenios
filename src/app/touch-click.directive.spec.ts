import { TouchClickDirective } from './touch-click.directive';

describe('TouchClickDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchClickDirective();
    expect(directive).toBeTruthy();
  });
});
